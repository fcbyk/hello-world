/**
 * 变量（Variables）
 * Kotlin 中变量分为两种：可变变量 和 不可变变量。
 */

/**
 * 不可变变量 — val
 * 一旦赋值，就不能再修改
 * 用 val 声明
 */
val name = "小米"         // 声明不可变字符串变量
// name = "新名字"         // 错误，val 不能重新赋值


/**
 * 可变变量 — var
 * 可以修改的变量
 */
var age = 18
age = 20        // 可以修改
