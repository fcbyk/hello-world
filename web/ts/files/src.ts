/**
 * .ts 文件
 * TypeScript 的主要源代码文件。
 * 用来编写包含 TypeScript 代码的文件，它可以包含 TypeScript 的类型检查、类、接口、函数等。
 */

let message: string = 'Hello, world!';
function greet(name: string): string {
  return `Hello, ${name}`;
}
